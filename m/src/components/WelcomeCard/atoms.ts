import { SomaCard } from '@soma/react';
import styled from 'styled-components';

export const MessageContainer = styled.div`
  margin: 20px 0;
`;

export const Card = styled(SomaCard)`
  width: 600px;
  max-width: 100%;
`;
