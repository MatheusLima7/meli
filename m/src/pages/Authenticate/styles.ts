import styled from 'styled-components';

export const Wrapper = styled.div`
  background: #10121A;
  position: absolute;
  display: flex;
  height: 100%;
  width: 100%;
  z-index: 100;
`;

export default Wrapper;
